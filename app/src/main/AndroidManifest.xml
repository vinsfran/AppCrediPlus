<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="py.com.desapy.appcrediplus">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
    <!--
 The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but are recommended.
    -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/txt_nombre_app"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyTheme">
        <activity android:name=".InicioActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginActivity"
            android:label="@string/txt_acceso_sistema"
            android:windowSoftInputMode="stateAlwaysHidden" />
        <activity
            android:name=".FormIngresoDocumentoActivity"
            android:label="@string/txt_ingreso_documento"
            android:windowSoftInputMode="stateAlwaysHidden" />
        <activity
            android:name=".FormIngresoPinActivity"
            android:label="@string/txt_ingreso_pin"
            android:windowSoftInputMode="stateAlwaysHidden" />
        <activity
            android:name=".FormDatosPersonaActivity"
            android:label="@string/txt_ingreso_alta_persona"
            android:windowSoftInputMode="stateAlwaysHidden" />

        <activity
            android:name=".Login3Activity"
            android:label="@string/title_activity_login3"></activity>
    </application>

</manifest>